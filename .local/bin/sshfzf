#!/bin/bash
SSHCONFIGPATH=$HOME/.ssh/config.d/

SELECTED=$(find $SSHCONFIGPATH -type f -exec sed -rn 's/^\s*Host\s+(.*)\s*/\1/ip' {} \; | cut -f1 -d' ' | fzf)

if [ -n "${SELECTED}" ]; then
	ssh $SELECTED "$@"
fi
